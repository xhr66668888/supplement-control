# Bio-Nutrient Manager 未来改进方向

## 1. 双曲空间嵌入与语义距离计算（已在 taxonomy.js 中初步实现）

当前实现：基于 ATC 树深度的 `sech` 函数置信度衰减 + `hyperbolicDistance` 语义距离函数。这模拟了庞加莱球模型中"体积随半径指数增长"的核心特性——树越深，兄弟节点之间的语义距离越大。

未来方向：
- 引入真正的双曲空间嵌入（Poincare ball / Lorentz model），将每个 ATC 代码和营养素映射到双曲空间中的向量
- 使用黎曼优化器（Riemannian Adam）训练嵌入，使父子节点在双曲空间中更近
- 对比学习（Hyperbolic Contrastive Learning）：通过 loss 函数强制"父亲-儿子"距离小于"无关节点"距离
- 这需要引入 PyTorch 或 TensorFlow 作为嵌入训练后端，通过 API 与 Node.js 通信

## 2. 药代动力学 PDE 建模

当前实现：硬编码的四时段服药排程（早晨空腹、随餐、午后、睡前），基于静态规则。

未来方向：
- 引入房室模型（Compartment Model）：胃肠道 → 血液 → 靶组织
- 使用常微分方程（ODE）建模血药浓度随时间的变化：`dC/dt = -k * C + input(t)`
- 对于多种补剂同时服用的情况，使用偏微分方程（PDE）建模吸收竞争
- 个性化参数：根据用户的年龄、肾功能（肌酐清除率）、肝功能调整代谢速率常数 k
- 例如：铁和钙竞争吸收的动力学可以用 Lotka-Volterra 竞争方程建模

## 3. 概率图模型用于疾病-营养素推理

当前实现：确定性规则引擎（ATC 树锚定 + 关键词匹配 + 临床上下文正则）。

未来方向：
- 贝叶斯网络：`P(nutrient | condition, age, gender, medications)`
- 利用真实用户反馈数据更新先验概率
- 马尔可夫随机场（MRF）建模营养素之间的相互作用网络
- 处理不确定性：当多个条件同时存在且推荐冲突时，使用概率推理而非确定性规则

## 4. 大规模真实数据训练

当前实现：种子数据 160 种营养素 + 35 个 ATC 条件 + 35 条相互作用规则，全部手工编写。

未来方向：
- 爬取 PubMed、NIH DSLD、日本 PMDA 等公开数据库
- 自动提取营养素-疾病-药物的关系三元组
- 使用 BioBERT / PubMedBERT 进行命名实体识别和关系抽取
- 构建知识图谱（Knowledge Graph），替代当前的静态 JSON 映射
- 用户反馈闭环：用户标记"这个推荐有用/无用"作为训练信号

## 5. 个性化吸收能力建模

当前实现：基于年龄和性别的 RDA 匹配，不考虑个体吸收差异。

未来方向：
- 根据用户的胃肠道疾病（IBD、胃炎、胃切除术后）调整生物利用度系数
- 例如：胃切除术后维生素 B12 吸收率下降 70%，需要相应提高推荐剂量
- 基因多态性：MTHFR C677T 变异影响叶酸代谢，需要推荐甲基叶酸而非普通叶酸
- 引入"吸收能力评分"（Absorption Score），综合胃肠道健康状况、年龄、药物使用

## 6. 补剂质量与生物利用度数据库

当前实现：只记录营养素名称和含量，不区分形式和生物利用度。

未来方向：
- 镁的不同形式：氧化镁（生物利用度 4%）vs 甘氨酸镁（生物利用度 80%）
- 钙的不同形式：碳酸钙（需胃酸）vs 柠檬酸钙（不需胃酸）
- 姜黄素：标准姜黄素 vs 磷脂复合物（Meriva，生物利用度提高 29 倍）
- 建立"生物利用度调整因子"数据库，在计算实际摄入量时自动调整

## 7. 时序数据分析与预警

当前实现：静态库存预警（30 天/7 天）。

未来方向：
- 根据用户的服用历史和依从性预测库存耗尽日期
- 检测不规律服用模式并发送提醒
- 血液检测结果集成：用户上传血检 PDF，自动提取营养素水平并调整推荐剂量
- 季节性调整：冬季维生素 D 需求增加、过敏季节槲皮素需求增加

## 8. 联邦学习与隐私保护

当前实现：所有数据存储在本地 SQLite，用户数据隔离。

未来方向：
- 联邦学习：在不共享原始用户数据的情况下，跨用户训练全局模型
- 差分隐私：在聚合统计数据时添加噪声，保护个体隐私
- 本地 AI：使用 ONNX Runtime 在用户设备上运行轻量级推理，减少云端依赖

## 9. 多语言药品名称识别

当前实现：elementResolver 支持中、英、法、德、意、西六种语言的别名映射。

未来方向：
- 使用多语言 BERT（XLM-RoBERTa）进行跨语言药品名称标准化
- 构建全球药品名称知识图谱（中国 CFDA、美国 FDA、欧盟 EMA、日本 PMDA）
- 支持日语、韩语、阿拉伯语、俄语等更多语言
- 使用音译模型（Transliteration）处理同一药品在不同语言中的拼写差异

## 10. 医学影像集成

当前实现：MIMO v2.5 Pro 仅用于补剂标签 OCR。

未来方向：
- 血液检测报告 OCR + 结构化
- 皮肤、头发、指甲照片分析（营养素缺乏的外在表现）
- 与可穿戴设备数据集成（Apple Health、Fitbit）：心率变异性、睡眠质量、活动量
- 使用这些信号动态调整补剂推荐
